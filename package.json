{
  "name": "aeon-workspace",
  "private": true,
  "version": "0.1.0",
  "description": "Aeon — a full-stack, signal-based web framework built for minimal load and maximum render speed.",
  "workspaces": [
    "packages/*",
    "examples/*",
    "benchmark/*"
  ],
  "scripts": {
    "build": "npm run build --workspaces --if-present",
    "build:standalone": "node scripts/build-standalone.mjs",
    "test": "node --test packages/core/test/*.test.mjs packages/migrate/test/*.test.mjs packages/http/test/*.test.mjs packages/testing/test/*.test.mjs packages/ssr/test/*.test.mjs packages/animate/test/*.test.mjs packages/i18n/test/*.test.mjs packages/cli/test/*.test.mjs packages/devtools/test/*.test.mjs packages/mcp/test/*.test.mjs packages/router/test/*.test.mjs packages/ssg/test/*.test.mjs",
    "portability-check": "node scripts/portability-check.mjs",
    "demo": "node packages/cli/bin/aeon.mjs dev examples/demo-app"
  },
  "devDependencies": {
    "esbuild": "^0.24.0"
  },
  "engines": {
    "node": ">=18"
  }
}
